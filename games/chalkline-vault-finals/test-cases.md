# Chalkline Vault Finals Test Cases

- 타이틀에서 시작 버튼과 일일 바람·최고 기록이 보인다.
- Start → Routine Call에서 Pike/Twist/Blind Moon 세 선택지가 동작한다.
- 도마판 드래그/릴리스가 거리 기반 속도·각속도를 만든다.
- 비행 중 pointer Y 변화가 tuck·몸 반경·각속도를 연속 변경한다.
- 착지 시 Judge's Freeze, 점수, 판정이 보인다.
- 2·3라운드는 바람/착지창이 더 불리하다.
- 세 번 뒤 합계·메달·재시작이 보인다.
- localStorage에 bestTotal, bestVault, runs, daily가 저장된다.
- `?qa=1`에서 `window.__qaResult.pass === true`다.
- 390x844에서 가로 넘침, `undefined`, `NaN`, JS pageerror가 없다.
