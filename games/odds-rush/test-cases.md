# Odds Rush Test Cases

| ID | 영역 | 절차 | 기대 결과 |
|---|---|---|---|
| TC-01 | 타이틀 | 기본 주소를 연다 | 제목 "오즈 러시", 규칙 설명, BEST 2종(점수/최대연속), `시작` 버튼, 연구 원천 인용이 보인다 |
| TC-02 | 시작 | `시작`을 누른다 | HUD(시간/점수/연속/라운드), 타이머 바, 출주표 6마리, 힌트 카드 3종이 나타난다 |
| TC-03 | 출주표 | 각 마리를 탭한다 | 선택한 마리에 복병 선택(⭐) 표시, 이전 선택은 해제된다 |
| TC-04 | 필러 제거 | 마리를 길게 누른다 | 필러 제거(🚫) 표시 + 반투명 처리, 복병 선택 해제 안 됨 |
| TC-05 | 힌트 카드 | 힌트 카드를 탭한다 | 잠금 해제 → v6 사례의 판별 포인트 텍스트 + 출처 표시 |
| TC-06 | 복병 확정 | 복병을 선택 후 `복병 확정` | 타이머 정지, 결과 화면(적중/오답 + 점수 + 연속 정보) |
| TC-07 | 점수 계산(정답) | 고배당 복병을 빨리 맞춘다 | 점수 = odds×10 + streak×5 + timeLeft×2 |
| TC-08 | 점수 계산(오답) | 필러를 복병으로 선택한다 | 0점, 연속 0 리셋 |
| TC-09 | 5라운드 진행 | 5라운드를 모두 완료한다 | 최종 요약 화면(총점, 적중 수, 최대 연속, 라운드별 결과) |
| TC-10 | localStorage | 게임 완료 후 새로고침 | BEST 점수·최대 연속이 복원된다 |
| TC-11 | 타임아웃 | 30초를 초과한다 | 자동으로 현재 선택 확정 (미선택 시 0점) |
| TC-12 | 라운드 결정론 | 같은 시드로 2번 실행한다 | 같은 출주표·같은 배당·같은 정답 |
| TC-13 | 모바일 | 390×844 뷰포트에서 실행 | 가로 스크롤 없이 HUD·출주표·힌트가 안전영역 내에 있다 |
| TC-14 | QA 자동화 | `?qa=1`로 열고 1초 대기한다 | `window.__qaResult.pass === true` (15 어설션), `pageErrors=[]` |
| TC-15 | 텍스트 건전성 | 화면 텍스트를 검사한다 | `undefined`, `NaN` 문자열이 0개다 |
| TC-16 | 정적 검증 | 추출한 JS를 `node --check` 한다 | 구문 오류 0개, 헤더 주석에 연구 원천 3건 인용 존재 |
| TC-17 | 힌트 3종 확인 | 힌트 카드 라벨을 검사한다 | "전개 페이스", "주로 상태", "배당 흐름" 존재 |
| TC-18 | 런타임 흐름 QA | 런타임 하니스로 타이틀→5라운드→요약 실경로를 돌린다 | `28/28 PASS`, pageErrors=`[]`, 부트스트랩 정상 동작 |
| TC-19 | 부트스트랩 | 페이지 로드 직후 `S.phase`를 확인한다 | `S.phase==='title'` + 타이틀 화면이 렌더링된다 (빈 화면 금지) |
