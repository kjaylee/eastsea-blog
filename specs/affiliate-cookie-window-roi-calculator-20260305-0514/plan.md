# Plan — affiliate-cookie-window-roi-calculator

1. Create new slug folder and implement responsive calculator UI.
2. Implement isolated JS calculation/validation module for reliable syntax/test verification.
3. Add node test file for positive-economics and validation guard.
4. Wire discoverability entries in `tools/index.html`, `tools/index.md`, `tools/manifest.json`.
5. Run required checks: `node --check`, `node --test`, and local HTTP 200 proof.
