# Plan — apple-search-ads-cpt-ltv-roi-calculator

1. Build calculator UI and formulas in `index.html` + `script.js`.
2. Add responsive layout and concise monetization-focused copy.
3. Wire discovery entries in `tools/index.html` and `tools/index.md`.
4. Rebuild `tools/manifest.json` with `bash scripts/build-manifests.sh`.
5. Verify with `node --check` and local HTTP 200 curl proof.
6. Run quality loop and commit with prefix `Add monetization tool:`.
