# Plan — big5-workstyle-map

1. Extract scoring model into `logic.mjs`.
2. Replace inline-script UI with `app.mjs` orchestration.
3. Add unit tests for validation, tie-breaks, and summary outputs.
4. Create spec-gate docs and record verification evidence.
5. Rebuild manifests (if needed), run required verification chain, and commit only relevant files.

