# D1 단일 원장 전환 검증

## 구현 결과

- 홈페이지의 `MAX_POSTS = 500` 제한을 제거했다.
- 카테고리 필터를 D1 API 쿼리로 전달한다.
- API `total`과 실제 렌더 수가 같을 때 무한 스크롤을 종료한다.
- D1 전체 목록으로 `posts.json`, `sitemap.xml`, `rss.xml`, `atom.xml`, `feed.xml`을 배포 스테이지에서 생성한다.
- D1 응답 오류·빈 결과·중복 slug·총계 변화·총계 불일치 시 배포를 실패 종료한다.

## 테스트 증거

### 변경 특화 테스트

- Python 단위 테스트: 5/5 통과
- Node 통합 테스트: 4/4 통과
- `bash -n scripts/cf-pages-auto-deploy.sh`: 통과
- `node --check blog-api/src/index.js`: 통과
- `git diff --check`: 통과

### 실제 D1 투영

| 검증 | 결과 |
|---|---:|
| D1 선언 게시물 | 1,550 |
| 생성 `posts.json` | 1,550 |
| 고유 filename | 1,550 |
| 사이트맵 게시물 URL | 1,550 |
| 사이트맵 전체 URL | 2,579 |
| RSS 항목 | 50 |
| Atom 항목 | 50 |
| 최신 slug | `2026-07-16-us-market-close` |
| 가장 오래된 slug | `2026-01-28-daily-briefing` |

배포 스테이지 생성도 4,774개 파일로 성공했다.

### 전체 회귀 비교

- 변경본: 551건 중 514 통과, 31 실패
- 무변경 `origin/master`: 547건 중 510 통과, 31 실패
- 신규 테스트 4건이 추가로 통과했고 실패 수는 증가하지 않았다.
- 기존 31개 실패는 날짜 형식, 도구·게임 매니페스트 드리프트, 누락된 웹소설 파일, 핫딜 메타 불일치 등 이번 변경 밖의 기존 결함이다.
- Python 전체 탐색은 테스트 10건 중 5건이 기존 작업 트리 절대 경로를 하드코딩해 import 실패한다. 신규 투영 테스트 5건은 별도 실행에서 전부 통과했다.

## 품질 루프

| 회차 | 점수 | 조치 |
|---|---:|---|
| 1 | 82 | Cloudflare 403을 발견해 요청 헤더가 실제 요청 객체로 전달되도록 수정 |
| 2 | 88 | 배포 파이썬의 깨진 XML 확장 의존성을 제거 |
| 3 | 96 | 실제 D1 총계·고유 수·사이트맵·피드와 무변경 회귀 기준을 교차 검증 |

## 남은 위험

- 배포 순간 D1 데이터가 쓰이면 총계가 바뀌어 배포가 실패할 수 있다. 이는 누락된 투영을 배포하는 것보다 안전하며 다음 실행에서 회복된다.
- 공개 목록은 D1 가용성에 의존한다. 배포된 `posts.json`은 읽기 전용 장애 폴백으로 남는다.
- 저장소 전체의 기존 31개 회귀 실패는 별도 정합성 정리가 필요하다.
