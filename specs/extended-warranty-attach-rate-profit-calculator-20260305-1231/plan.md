# Plan

1. Create tool folder `tools/extended-warranty-attach-rate-profit-calculator/` with `index.html` + `calculator.js`.
2. Implement UI with input grid, KPI tiles, summary box, and status pill.
3. Implement calculation + validation logic in `calculator.js` per spec.
4. Wire tool into `tools/index.html`, `tools/index.md`, and `tools/manifest.json`.
5. Run verification: `node --check` on JS and HTTP 200 via local server + curl.
6. Fill verification + gap analysis with score and fixes if needed.
