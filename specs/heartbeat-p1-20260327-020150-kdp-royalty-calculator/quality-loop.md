# Quality Loop: `kdp-royalty-calculator`

1. Research the discovery gap and confirm platform-specific math from official KDP docs.
2. Build pure calculator logic first so the formulas are testable.
3. Add the UI only after calculation outputs are stable.
4. Verify HTML anchors, bilingual toggle behavior, and summary generation.
5. Run exact-once discovery assertions before touching git.
6. If localhost smoke is blocked, record the exact blocker rather than guessing.
