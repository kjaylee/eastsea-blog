# Test Cases: `kdp-royalty-calculator`

- `TC-KDP-01` 70% plan baseline with mixed eligible-territory sales and KU pages.
- `TC-KDP-02` 35% plan ignores delivery costs and territory split.
- `TC-KDP-03` VAT-inclusive pricing lowers net price and royalty per sale.
- `TC-KDP-04` Higher delivery cost reduces 70% royalty and can erase the 70% advantage.
- `TC-KDP-05` Target monthly royalty converts to required units with KU earnings offset.
- `TC-KDP-06` Target royalty-per-sale converts to required list price.
- `TC-KDP-07` Invalid inputs return errors instead of results.
- `TC-KDP-08` Summary includes core decision fields.
- `TC-KDP-09` HTML scaffold contains required anchors and scripts.
- `TC-KDP-10` Discovery files contain the slug exactly once.
