# Test Cases

- `TC-UDM-01` baseline scenario matches expected channel payouts, total payout, and take-home.
- `TC-UDM-02` changing partner share changes only partner payout and total payout as expected.
- `TC-UDM-03` subscription math respects pool % and minutes-share %.
- `TC-UDM-04` target helper returns zero extra promo sales when target is already met.
- `TC-UDM-05` invalid negative money / rate / share inputs are rejected.
- `TC-UDM-06` HTML scaffold contains required anchors and script wiring.
- `TC-UDM-07` discovery files contain the slug and URL exactly once after updates.
