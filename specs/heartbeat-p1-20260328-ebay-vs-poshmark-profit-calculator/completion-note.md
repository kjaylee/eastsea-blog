Built `ebay-vs-poshmark-profit-calculator` in eastsea-blog as a new resale marketplace comparator. Added static page + composition-layer JS + 9 passing Node tests, wired discovery surfaces (`tools/index.html`, `tools/index.md`, `_data/tools-list.json`, regenerated `tools/manifest.json`), and verified local HTTP 200 render. Key modeling choice: compare the same realized item price across platforms while keeping eBay shipping/tax/promoted-rate assumptions explicit.