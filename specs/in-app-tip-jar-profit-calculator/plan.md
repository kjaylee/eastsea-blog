# Plan — In-app Tip Jar Profit Calculator

1. Create new tool directory `tools/in-app-tip-jar-profit-calculator/` with `index.html` + `app.mjs`.
2. Implement calculator UI (inputs, KPIs, details table, summary copy) with responsive CSS.
3. Implement calculation + validation logic in `app.mjs`.
4. Wire tool into `tools/index.html`, `tools/index.md`, `tools/manifest.json`.
5. Compute `index.html` byte size for manifest.
6. Run verification commands (node check + local HTTP 200).
7. Complete quality scoring and gap analysis.
