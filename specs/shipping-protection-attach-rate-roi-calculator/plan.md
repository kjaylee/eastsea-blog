# Plan — Shipping Protection Attach Rate ROI Calculator

1. Create new tool folder under tools/ with index.html, app.mjs, logic.mjs.
2. Implement calculation logic and validation in logic.mjs.
3. Wire UI interactions, summary copy, and localStorage persistence in app.mjs.
4. Add tool card to tools/index.html and add entry to tools/index.md.
5. Add entry to tools/manifest.json with computed size.
6. Verify with node --check on JS files and HTTP 200 via local server + curl.
7. Run quality loop and record verification/gap analysis.
8. Commit changes with required message prefix.
