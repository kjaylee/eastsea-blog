# Gap Analysis — TikTok Shop Fee Calculator

## Spec vs Implementation
- Inputs: item price, shipping, tax, platform discount, referral rate (standard/promo/custom), custom rate, processing rate/fixed, orders/month → ✅ implemented.
- Outputs: per-order snapshot + monthly projection → ✅ implemented.
- Calculation rules: referral base formula + fee computations → ✅ implemented.
- UX: responsive single-page, no external deps, reset button, notes/disclaimer → ✅ implemented.

## Gaps
- None found against current spec.
