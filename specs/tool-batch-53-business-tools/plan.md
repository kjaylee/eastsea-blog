# Plan — Tool Batch 53 Business Productivity

1. Create/refresh tool directories and single-page implementations.
2. Apply consistent UI shell (header, KPI cards, responsive grid, Back to Portal link).
3. Implement core formulas per tool and edge-case validation.
4. Smoke-test by opening key files and running quick JS syntax check where possible.
5. Regenerate `tools/manifest.json` via `scripts/build-manifests.sh`.
6. Commit/push changes with requested message.
7. Deploy on MiniPC and refresh `tools-list.json`.
